# v3: LoRA 微调配置。
lora:
  enabled: true
  rank: 8
  alpha: 16
  dropout: 0.0
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj

train:
  freeze_base_model: true
  adapter_dir: adapters/default

