# v0: 最小 Transformer + FFN 主干配置。
model:
  vocab_size: 1024
  max_seq_len: 128
  n_layers: 4
  n_heads: 4
  hidden_size: 256
  ffn_hidden_size: 1024
  dropout: 0.0

train:
  batch_size: 8
  learning_rate: 0.0003
  max_steps: 200
  seed: 42

