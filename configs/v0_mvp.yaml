# v0: 最小 Transformer + FFN 主干配置。
model:
  vocab_size: 32000
  max_seq_len: 2048
  n_layers: 10
  n_heads: 20
  hidden_size: 2560
  ffn_hidden_size: 6912
  dropout: 0.0
  tie_embeddings: true

train:
  batch_size: 8
  learning_rate: 0.0003
  max_steps: 200
  seed: 42
